# Connects to GVIP backend (FastAPI in ../backend) at http://localhost:8000
# Leave empty in dev — Vite proxies /api/v1 and /health to port 8000 (see vite.config.js)
# Do NOT use the Flask backend in backend/ (port 5000) with this frontend.
VITE_API_URL=